{
  "name": "browserl.ist",
  "private": true,
  "homepage": "https://browsersl.ist/",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "pnpm -r start",
    "prebuild": "pnpm -r prebuild",
    "build": "pnpm -r build",
    "lint": "eslint . && stylelint **/*.css",
    "test:run": "node check-versions.js && pnpm audit --prod && pnpm lint",
    "test": "pnpm -r --include-workspace-root test:run"
  },
  "dependencies": {
    "ssdeploy": "^0.9.1"
  },
  "devDependencies": {
    "@logux/eslint-config": "^47.2.0",
    "@logux/stylelint-config": "^0.10.1",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^8.21.0",
    "eslint-config-standard": "^17.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-n": "^15.2.4",
    "eslint-plugin-prefer-let": "^3.0.1",
    "eslint-plugin-promise": "^6.0.0",
    "nano-staged": "^0.8.0",
    "prettier": "^2.7.1",
    "simple-git-hooks": "^2.8.0",
    "stylelint": "^14.10.0",
    "svgo": "^2.8.0",
    "typescript": "^4.7.4"
  }
}
