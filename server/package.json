{
  "name": "browserl.ist-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test:run": "pnpm -r build && node --test test/*.test.js",
    "test": "pnpm test:run"
  },
  "dependencies": {
    "browserslist": "^4.21.3",
    "caniuse-lite": "^1.0.30001375"
  }
}
