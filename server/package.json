{
  "name": "browserl.ist-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "pnpm build-static && node app.js",
    "build-static": "pnpm -r build",
    "test": "pnpm build-static && node --test test/*.test.js"
  },
  "dependencies": {
    "browserslist": "^4.21.3",
    "caniuse-lite": "^1.0.30001374"
  }
}
