{
  "name": "browserl.ist-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "pnpm static-watch && node app.js",
    "static-watch": "pnpm -r build:watch",
    "test": "pnpm static-watch && node --test test/*.test.js"
  },
  "dependencies": {
    "browserslist": "^4.21.3",
    "caniuse-lite": "^1.0.30001374"
  }
}
